#class   HabBuy :

#def calculateSpendDaySum(self, total_transaction_day, total_transaction):
#	    total_transaction_day = total_transaction_day + total_transaction
#	    return total_transaction_day

#	def updateHabBuyDay(self, tab, nb_of_day):
#		nb_of_day = nb_of_day - 1
#		tab[nb_of_day] = tab[nb_of_day] + 1
#		return tab

#	def updateDayofTransaction(self):
#		if self.date_buy == self.old_date_buy :
#			self.total_transaction_day = self.calculateSpendDaySum(self.total_transaction_day, self.total_transaction)
#		else :
#			self.total_transaction_day = 0
#			self.total_transaction_day = self.calculateSpendDaySum(self.total_transaction_day, self.total_transaction)

#def itemValidation(self, item, list):
#		for x in list :
#			if x == item :
#				return bool(True)
#		return bool(False)